// Generated by oblodai-backend tools/sdkgen from openapi.json - do not edit.
/**
 * The signing protocol of the contract (`x-oblodai-signing`): header names by role, the canonical
 * strings and the limits the core enforces. Internal to the SDK; the public names are derived from
 * these, so a header the core renames reaches the SDK by regeneration alone.
 */

/** The MAC over a canonical string: HMAC-SHA256, lowercase hex. */
export const SIGNATURE_ALGORITHM = "HMAC-SHA256, lowercase hex";

/** Headers of a signed request, by role. */
export const REQUEST_HEADERS = {
  publicId: "X-Public-Id",
  signature: "X-Signature",
  timestamp: "X-Timestamp",
  idempotencyKey: "Idempotency-Key",
} as const;

/** Parts of the request canonical string, in order, joined by `REQUEST_CANONICAL_SEPARATOR`. */
export const REQUEST_CANONICAL_PARTS = [
  "ts",
  "METHOD",
  "request_uri",
  "idempotency_key",
  "body",
] as const;
export const REQUEST_CANONICAL_SEPARATOR = "\n";

/** Headers of a webhook delivery, by role. */
export const WEBHOOK_HEADERS = {
  timestamp: "X-Webhook-Timestamp",
  signature: "X-Webhook-Signature",
  signaturePrev: "X-Webhook-Signature-Prev",
  event: "X-Webhook-Event",
  id: "X-Webhook-Id",
  eventId: "X-Webhook-Event-Id",
  eventTime: "X-Webhook-Event-Time",
} as const;

/** Parts of the webhook canonical string, in order, joined by `WEBHOOK_CANONICAL_SEPARATOR`. */
export const WEBHOOK_CANONICAL_PARTS = ["ts", "payload"] as const;
export const WEBHOOK_CANONICAL_SEPARATOR = ".";

/** Clock skew the core accepts on a request timestamp, seconds; also a receiver's delivery window. */
export const SKEW_SECONDS = 300;

/** The largest request body the core reads, bytes. */
export const MAX_BODY_BYTES = 1048576;

/** The longest idempotency key the core accepts, characters. */
export const MAX_IDEMPOTENCY_KEY_LENGTH = 255;
